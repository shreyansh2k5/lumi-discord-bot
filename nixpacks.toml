# nixpacks.toml
# Tells Railway's build system to install FFmpeg alongside Python.
# Without this, voice audio won't work.

[phases.setup]
nixPkgs = ["ffmpeg", "python312"]

[start]
cmd = "python main.py"
